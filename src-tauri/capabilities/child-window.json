{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "child-window",
  "description": "Capability for child windows opened via shell.openWindow — only enough to report an evalWindow/getWindowBody result back",
  "windows": ["shell-window-*"],
  "remote": {
    "urls": ["http://*/*", "https://*/*"]
  },
  "permissions": [
    "shell-eval-result"
  ]
}
